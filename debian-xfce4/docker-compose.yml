services:
  # docker exec -it debian-desktop-xfce4-1 passwd debian
  # podman exec -it debian-desktop_xfce4_1 passwd debian
  xfce4:
    image: docker.io/meteor1113/debian-xfce4
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "2422:22"
      - "3489:3389"
    privileged: true
    # volumes:
    #   - /sys/fs/cgroup:/sys/fs/cgroup:ro
    #   - /run:/run:rw
    #   - /tmp:/tmp
    # tmpfs:
    #   - /run
    #   - /run/lock
    #   - /tmp
