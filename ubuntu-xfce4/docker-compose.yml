services:
  # docker exec -it ubuntu-xfce4-desktop-1 passwd ubuntu
  # podman exec -it ubuntu-xfce4_desktop_1 passwd ubuntu
  desktop:
    image: docker.io/meteor1113/ubuntu-xfce4
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    restart: always
    ports:
      - "4022:22"
      - "4389:3389"
    privileged: true
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - /:/mnt/host
    #   - /sys/fs/cgroup:/sys/fs/cgroup:ro
    #   - /run:/run:rw
    #   - /tmp:/tmp
    # tmpfs:
    #   - /run
    #   - /run/lock
    #   - /tmp
