services:
  # docker exec -it ubuntu-desktop-xfce4-1 passwd ubuntu
  # podman exec -it ubuntu-desktop_xfce4_1 passwd ubuntu
  xfce4:
    image: docker.io/meteor1113/ubuntu-xfce4
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    restart: always
    ports:
      - "4022:22"
      - "4389:3389"
    privileged: true
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - /:/mnt/host
    #   - /sys/fs/cgroup:/sys/fs/cgroup:ro
    #   - /run:/run:rw
    #   - /tmp:/tmp
    # tmpfs:
    #   - /run
    #   - /run/lock
    #   - /tmp
